# Timeline Builder

A self-contained HTML tool to build timelines from many templates, theme them, and export to PNG, PDF, and Excel (.xlsx with embedded image).

Open `index.html` in a browser — no install, no dependencies.
